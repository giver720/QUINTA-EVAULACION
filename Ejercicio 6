#include <iostream>
using namespace std; 

int main () {
	
int numeros[]={4, 0, 8, 0, 2, 9, 0, 5, 1, 0};
int n=10;
    

 cout << "Arreglo original: ";
for(int i = 0; i < n; i++) {
cout << numeros[i] << " ";
    }
    cout << endl;

  int indiceValido = 0; 


for(int i = 0; i < n; i++) {
    if(numeros[i] != 0) {
   numeros[indiceValido] = numeros[i];
   indiceValido++;
     }
    }
    
while(indiceValido < n) {
numeros[indiceValido] = 0;
indiceValido++;
    }
    
 cout << "Arreglo modificado: ";
    for(int i = 0; i < n; i++) {
        cout << numeros[i] << " ";
    }
    cout << endl;
    
    return 0;
}
